<?xml version="1.0" encoding="UTF-8"?>
<schema xmlns="http://www.w3.org/2001/XMLSchema" xmlns:wpvs="http://www.deegree.org/services/wpvs"
  targetNamespace="http://www.deegree.org/services/wpvs" elementFormDefault="qualified">
  <element name="ServiceConfiguration">
    <annotation>
      <documentation>
        Top root element of the service configuration, it defines rendering parameters, get view request
        restrictions as well as datasets and elevation models.
      </documentation>
    </annotation>
    <complexType>
      <sequence>
        <!-- options for the caching -->
        <!-- Configures the number of textures stored in GPU (Graphic Processing Unit) memory, e.g. ready for rendering. -->
        <element name="TexturesInGPUMem" type="int" default="300" minOccurs="0" />
        <!-- The number of texture tiles (a dataset texture for a dem fragment) to be cached -->
        <element name="CachedTextureTiles" type="int" default="400" minOccurs="0" />
        <!-- The maximum direct memory (in MegaBytes) to be allocated for texture coordinates -->
        <element name="DirectTextureMemory" type="int" default="400" minOccurs="0" />
        <!--
          The number of dem fragments stored in memory, this value depends on following variables: <ul> <li>width/height
          of the elevationmodel</li> <li>the number of levels in a macrotriangle</li> <li>the number of rows in a
          macrotriangle</li> </ul> <p>for a dem with 32768*32768 meters, 18 levels and 64 rows per macro triangle 1000
          is good value.</p> <p>a dem with 32768*32768 meters, with 16 levels and 128 rows per macro 300 is sufficient.
          </p> <p>In general, the higher the value, the more memory is needed, the less IO is needed.</p>
        -->
        <element name="NumberOfDEMFragmentsCached" type="int" default="1000" minOccurs="0" />
        <!-- The maximum direct memory (in MegaBytes) to be allocated for reading mesh fragments from the disk -->
        <element name="DirectIOMemory" type="int" default="500" minOccurs="0" />
        <!-- How many direct buffers should be held in cache, they will be used for the result images -->
        <element name="NumberOfResultImageBuffers" type="int" default="25" minOccurs="0" />
        <!-- the approximate latitude of the scene in WGS84 degrees -->
        <element name="LatitudeOfScene" type="double" default="51.7" minOccurs="0" />
        <!-- maximum view width that can be requested. default = 1200 -->
        <element name="MaxViewWidth" type="int" default="1200" minOccurs="0" />
        <!-- maximum view height that can be requested. default = 1000 -->
        <element name="MaxViewHeight" type="int" default="1000" minOccurs="0" />
        <!--
          maximum value of the farclipping plane which might be requested. default = 15000 (units)
        -->
        <element name="MaxRequestFarClippingPlane" type="double" default="100000" minOccurs="0" />
        <!--
          The value of the near clipping plane independent of the request, default = 0.1 (units)
        -->
        <element name="NearClippingPlane" type="double" default="0.1" minOccurs="0" />
        <element ref="wpvs:Copyright" minOccurs="0" />
        <element ref="wpvs:SkyImages" />
        <!-- A file which contains information for the initialization of opengl (rb: the format is yet to be defined.) -->
        <element name="OpenGLInitConfigFile" type="string" minOccurs="0" />
        <element ref="wpvs:DatasetDefinitions" />
      </sequence>
    </complexType>
  </element>
  <element name="SkyImages">
    <annotation>
      <documentation>Defines a list of images which can be rendered in the background of the scene, e.g. the sky
      </documentation>
    </annotation>
    <complexType>
      <sequence>
        <element name="SkyImage" maxOccurs="unbounded">
          <complexType>
            <attribute name="name" type="string" />
            <attribute name="file" type="anyURI" />
          </complexType>
        </element>
      </sequence>
    </complexType>
  </element>
  <element name="Copyright">
    <annotation>
      <documentation>Defines a string or an image which should be rendered into the resulting GetView response image
      </documentation>
    </annotation>
    <complexType>
      <sequence>
        <element name="Text" type="string" default="Made by deegree WPVS" />
        <element name="Image">
          <complexType>
            <attribute name="url" type="anyURI" />
          </complexType>
        </element>
      </sequence>
      <attribute name="percentageOfResult" type="double" default="10" />
    </complexType>
  </element>
  <complexType name="AbstractDataType" abstract="true">
    <annotation>
      <documentation>
        A data type defines some properties like name, title and descriptions. A data type can have a
        bounding box, defining it's
        validity. The scale
        denominators are used to define the scales for which a data
        instance is valid.
      </documentation>
    </annotation>
    <sequence>
      <!--
        The title is identity building for a data type, just like the wms 1.3.0
        specification
      -->
      <element name="Title" type="string" />
      <element name="Name" type="string" minOccurs="0" />
      <element name="MetadataURL" type="string" minOccurs="0" maxOccurs="unbounded" />
      <element name="Abstract" type="string" minOccurs="0" />
      <element ref="wpvs:Keywords" minOccurs="0" maxOccurs="unbounded" />
    </sequence>
  </complexType>
  <element name="DatasetDefinitions">
    <annotation>
      <documentation>A dataset not visible through the capabilities and can not be requested by a client.
      </documentation>
    </annotation>
    <complexType>
      <sequence>
        <element name="Title" type="string" />
        <element name="MetadataURL" type="string" minOccurs="0" maxOccurs="unbounded" />
        <element name="Abstract" type="string" minOccurs="0" />
        <element ref="wpvs:Keywords" minOccurs="0" />
        <element name="BaseCRS" type="string" />
        <element ref="wpvs:TranslationToLocalCRS" />
        <!--
          Maximum acceptable pixel error (geometric deviation of projected points in the rendered image). This parameter
          determines when geometry LODs or objects are to be rendered are switched.
        -->
        <element name="MaxPixelError" type="double" default="5.0" minOccurs="0" />
        <element ref="wpvs:ElevationDataset" />
        <element ref="wpvs:DEMTextureDataset" minOccurs="0" maxOccurs="unbounded" />
        <element ref="wpvs:ColormapDataset" minOccurs="0" maxOccurs="unbounded" />
        <sequence>
          <element ref="wpvs:Buildings" minOccurs="0" maxOccurs="unbounded" />
          <element ref="wpvs:Trees" minOccurs="0" maxOccurs="unbounded" />
        </sequence>
      </sequence>
    </complexType>
  </element>
  <element name="TranslationToLocalCRS">
    <complexType>
      <attribute name="x" type="double" />
      <attribute name="y" type="double" />
    </complexType>
  </element>
  <element name="ElevationDataset">
    <annotation>
      <documentation>
        An elevation model is visible through the capabilities and can be requested by a client.
        Normally it
        will define a DEM
        (Digital Elevation
        Model) but it might as well have a more semantic character (using
        for example
        rent prices of houses).
      </documentation>
    </annotation>
    <complexType>
      <complexContent>
        <extension base="wpvs:AbstractDataType">
          <sequence>
            <!--
              Maximum acceptable pixel error (geometric deviation of projected points in the rendered image). This
              parameter determines when geometry LODs or objects are to be rendered are switched.
            -->
            <element name="MaxPixelError" type="double" default="5.0" minOccurs="0" />
            <element name="ElevationModelDataSource" type="string" />
          </sequence>
        </extension>
      </complexContent>
    </complexType>
  </element>
  <element name="ColormapDataset">
    <annotation>
      <documentation>
        An colormap which can be applied to an elevation model and can be requested by a client. The
        colors
        will be lineary interpreted between the min and max color.
      </documentation>
    </annotation>
    <complexType>
      <complexContent>
        <extension base="wpvs:AbstractDataType">
          <sequence>
            <!-- a hex decimal rgb string starting with a '#' if failing red will be used-->
            <element name="MinColor" type="string" minOccurs="0" />
            <!-- a hex decimal rgb string starting with a '#', if failing green will be used-->
            <element name="MaxColor" type="string" minOccurs="0" />
            <!-- a hex decimal rgb string starting with a '#', if failing a brownish color will be used-->
            <element name="HeightISOColor" type="string" minOccurs="0" />
            <!--the minimum value to which colors will be mapped, if failing the dem min bbox will be used-->
            <element name="MinZValue" type="double" minOccurs="0" />
            <!--the maximum value to which colors will be mapped, if failing the dem min bbox will be used-->
            <element name="MaxZValue" type="double" minOccurs="0" />
          </sequence>
        </extension>
      </complexContent>
    </complexType>
  </element>
  <group name="TextureProviders">
    <annotation>
      <documentation>A group of texture providers, which can be requested to receive textures from.
      </documentation>
    </annotation>
    <choice>
      <element name="CoverageDatasourceId" type="string" />
      <element ref="wpvs:StyledGeometryProvider" />
    </choice>
  </group>
  <element name="DEMTextureDataset">
    <annotation>
      <documentation>
        A data type defines some properties like name, title and descriptions. A data type can have a
        bounding box, defining it's
        validity. The scale
        denominators are used to define the scales for which a data
        instance is valid.
      </documentation>
    </annotation>
    <complexType>
      <complexContent>
        <extension base="wpvs:AbstractDataType">
          <sequence>
            <!--
              Maximum acceptable pixel error (geometric deviation of projected points in the rendered image). This
              parameter determines when geometry LODs or objects are to be rendered are switched.
            -->
            <element name="MaxPixelError" type="double" default="5.0" minOccurs="0" />
            <!--
              Maximum acceptable size (in pixels) that a texel of a lower-quality texture may cover in the rendered
              image. This parameter determines when texture LODs are switched.
            -->
            <element name="MaxProjectedTexelSize" type="double" default="5.0" minOccurs="0" />
            <group ref="wpvs:TextureProviders" />
          </sequence>
          <attribute name="RequestTimeout" type="int" default="30" />
        </extension>
        <!-- number of seconds to time out request to the datasource. -->
      </complexContent>
    </complexType>
  </element>
  <element name="StyledGeometryProvider">
    <annotation>
      <documentation>
        Defines a texture for very closed up views of the terrrain, for this purpose an SE-Style file
        will
        be evaluated, containing
        information about a data source having geometries and style information.
      </documentation>
    </annotation>
    <complexType>
      <sequence>
        <element name="TextureCacheDir" minOccurs="0">
          <complexType>
            <simpleContent>
              <extension base="string">
                <attribute name="CacheSize" type="double" use="optional" default="5" />
              </extension>
              <!-- The size in gigabytes -->
            </simpleContent>
          </complexType>
        </element>
        <element name="SE-StyleFile" type="string" />
        <element name="FeatureStoreId" type="string" />
      </sequence>
      <attribute name="MinimumUnitsPerPixel" type="double" use="optional" default="0.1" />
      <!--
        The number of units one texture pixel (texel) must have before this texture dataset becomes active. Normally
        this should be rather small number, smaller then the fines multiresolution level of 'normal' textures.
      -->
    </complexType>
  </element>
  <group name="FeatureDataSources">
    <annotation>
      <documentation>A group of datasources which can be requested to receive features (buildings, trees etc.) from.
      </documentation>
    </annotation>
    <choice>
      <element name="ConstrainedDatabaseDataSourceId" type="string" />
      <element name="GeospatialFileSystemDataSourceId" type="string" />
    </choice>
  </group>
  <complexType name="AbstractFeatureDatasetType" abstract="true">
    <complexContent>
      <extension base="wpvs:AbstractDataType">
        <sequence>
          <!--
            Maximum acceptable pixel error (geometric deviation of projected points in the rendered image). This
            parameter determines when geometry LODs or objects are to be rendered are switched.
          -->
          <element name="MaxPixelError" type="double" default="5.0" minOccurs="0" />
          <element name="TextureDirectory" type="anyURI" minOccurs="0" maxOccurs="unbounded" />
          <!--
            Each dataset can have 'child' datasets, which can be bound to other datasources.
          -->
          <group ref="wpvs:FeatureDataSources" minOccurs="0" maxOccurs="unbounded" />
        </sequence>
      </extension>
    </complexContent>
  </complexType>
  <element name="Buildings">
    <annotation>
      <documentation>Defines the values for the requestable building dataset.</documentation>
    </annotation>
    <complexType>
      <complexContent>
        <extension base="wpvs:AbstractFeatureDatasetType">
          <sequence>
            <element ref="wpvs:SwitchLevels" />
            <element ref="wpvs:Buildings" minOccurs="0" maxOccurs="unbounded" />
          </sequence>
        </extension>
      </complexContent>
    </complexType>
  </element>
  <element name="SwitchLevels">
    <annotation>
      <documentation>
        Defines the number of pixels one meter of a building must have, before switching to the next
        level
        of detail. Each level
        defines a
        quality level (starting with 0) where 0 is the most coarse level.
      </documentation>
    </annotation>
    <complexType>
      <sequence>
        <element name="Level" minOccurs="0" maxOccurs="unbounded">
          <complexType>
            <attribute name="min" type="double" use="required" />
            <attribute name="max" type="double" use="required" />
          </complexType>
        </element>
      </sequence>
    </complexType>
  </element>
  <element name="Trees">
    <annotation>
      <documentation>Defines the values for the requestable tree dataset.</documentation>
    </annotation>
    <complexType>
      <complexContent>
        <extension base="wpvs:AbstractFeatureDatasetType">
          <sequence>
            <element ref="wpvs:Trees" minOccurs="0" maxOccurs="unbounded" />
          </sequence>
        </extension>
      </complexContent>
    </complexType>
  </element>
  <!-- ========================================================== -->
  <element name="Keywords" type="wpvs:KeywordsType" />
  <!-- ========================================================== -->
  <complexType name="LanguageStringType">
    <annotation>
      <documentation>
        Text string with the language of the string identified
        as recommended in the XML 1.0 W3C
        Recommendation, section 2.12.
      </documentation>
    </annotation>
    <simpleContent>
      <extension base="string">
        <attribute name="lang" type="string" use="optional" />
      </extension>
    </simpleContent>
  </complexType>
  <complexType name="CodeType">
    <annotation>
      <documentation>
        Name or code with an (optional) authority. If the
        codeSpace attribute is present, its value shall
        reference a dictionary,
        thesaurus, or
        authority for the name or code, such as the organisation
        who assigned the
        value, or the dictionary from which it is taken.
      </documentation>
      <documentation>
        Type copied from basicTypes.xsd of GML 3 with
        documentation edited, for possible use outside the
        ServiceIdentification
        section of a service
        metadata document.
      </documentation>
    </annotation>
    <simpleContent>
      <extension base="string">
        <attribute name="codeSpace" type="anyURI" use="optional" />
      </extension>
    </simpleContent>
  </complexType>
  <complexType name="KeywordsType">
    <annotation>
      <documentation>
        Unordered list of one or more commonly used or
        formalised word(s) or phrase(s) used to describe the
        subject. When
        needed, the optional "type"
        can name the type of the associated list of
        keywords that shall all have
        the same type. Also when needed, the
        codeSpace attribute of that
        "type" can reference the type name authority
        and/or thesaurus. If the xml:lang attribute is not included in a Keyword
        element, then no
        language is specified
        for that element unless specified
        by another means. All Keyword elements in the same Keywords element that
        share
        the
        same xml:lang attribute value represent different keywords in
        that language.
      </documentation>
      <documentation>
        For OWS use, the optional thesaurusName element was
        omitted as being complex information that could
        be referenced by the
        codeSpace attribute
        of the Type element.
      </documentation>
    </annotation>
    <sequence>
      <element name="Keyword" type="wpvs:LanguageStringType" maxOccurs="unbounded" />
      <element name="Type" type="wpvs:CodeType" minOccurs="0" />
    </sequence>
  </complexType>
</schema>
