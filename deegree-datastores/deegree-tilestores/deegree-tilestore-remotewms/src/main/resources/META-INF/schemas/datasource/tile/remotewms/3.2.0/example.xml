<?xml version="1.0"?>
<RemoteWMSTileStore xmlns="http://www.deegree.org/datasource/tile/remotewms" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.deegree.org/datasource/tile/remotewms remotewms.xsd" configVersion="3.2.0">

  <!-- [1]: Id of the remote OWS to connect to (must be a WMS instance) -->
  <RemoteWMSId>wms1</RemoteWMSId>

  <TileDataSet>
    <Identifier>satellite</Identifier>
    <!-- [1]: Id entspricht dem Dateinamen der TileMatrixSet XML-Konfigurationsdatei -->
    <TileMatrixSetId>InspireCrs84Quad</TileMatrixSetId>
    <!-- [1]" image output format -->
    <OutputFormat>image/png</OutputFormat>
    <!-- [1]: WMS request parameters -->
    <RequestParams>
      <!-- [1] Comma-separated list of layer names -->
      <Layers>SatelliteProvo</Layers>
      <!-- [0..1] Comma-separated list of styles -->
      <Styles>default</Styles>
      <!-- [1] Image format, default png if availabe -->
      <Format>image/png</Format>
      <!-- CRS for querying the remote service -->
      <CRS>EPSG:4326</CRS>
    </RequestParams>
  </TileDataSet>

  <TileDataSet>
    <Identifier>roads</Identifier>
    <!-- [1]: Id entspricht dem Dateinamen der TileMatrixSet XML-Konfigurationsdatei -->
    <TileMatrixSetId>InspireCrs84Quad</TileMatrixSetId>
    <!-- image output format -->
    <OutputFormat>image/png</OutputFormat>
    <!-- [1]: WMS request parameters -->
    <RequestParams>
      <!-- [1] Comma-separated list of layer names -->
      <Layers>Roads</Layers>
      <!-- [0..1] Comma-separated list of styles -->
      <Styles>default</Styles>
      <!-- [1] Image format, default png if availabe -->
      <Format>image/png</Format>
      <!-- CRS for querying the remote service -->
      <CRS>EPSG:4326</CRS>
    </RequestParams>
  </TileDataSet>

</RemoteWMSTileStore>
